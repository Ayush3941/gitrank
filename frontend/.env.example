# Frontend BFF target (Go api-gateway)
GITRANK_API_BASE_URL=http://localhost:8080

# Frontend auth proxy target (Go auth-service)
GITRANK_AUTH_BASE_URL=http://localhost:8081

# Browser CSRF cookie name expected from auth-service
NEXT_PUBLIC_GITRANK_CSRF_COOKIE_NAME=gitrank_csrf

# Optional: override if backend session cookie name differs from default
AUTH_SESSION_COOKIE_NAME=gitrank_session

# Optional ABRA insight generation (server-side route: app/api/ai/abra-insights)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
